# =============================================================================
# myIoTGrid Cloud - Environment Variables
# =============================================================================
# Copy this file to .env and adjust values for your environment
# =============================================================================

# PostgreSQL Configuration
POSTGRES_PASSWORD=changeme

# Database Connection (for external DB, leave defaults for local postgres)
CLOUD_DB_HOST=postgres
CLOUD_DB_NAME=myiotgrid_cloud
CLOUD_DB_USER=myiotgrid

# API Configuration
ASPNETCORE_ENVIRONMENT=Production
